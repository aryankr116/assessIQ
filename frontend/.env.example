# Set to "real" to call a live FastAPI backend, or "mock" (default) to use built-in demo data.
VITE_API_MODE=mock

# Base URL of the FastAPI backend (used only when VITE_API_MODE=real).
VITE_API_BASE_URL=http://localhost:8000
